Necesito que configures mi proyecto en Replit para integrar una API existente con la API de Shopify, de modo que al hacer clic en el botón "Continuar" en mi página web ya diseñada, se genere un enlace de pago inmediato basado en las cuotas seleccionadas. Ya tengo un proyecto en Replit con una página web (index.html y script.js) que muestra cuotas con checkboxes y un resumen de pago, y un backend básico en Node.js. Las credenciales de Shopify (SHOPIFY_API_KEY, SHOPIFY_API_SECRET, SHOPIFY_ACCESS_TOKEN, SHOPIFY_STORE_URL) ya están agregadas manualmente en "Secrets". Sigue estas instrucciones exactas en orden para implementarlo desde este punto:

Crea un archivo llamado server.js en la raíz del proyecto si no existe.
Abre el archivo package.json. Si no existe, crea uno ejecutando npm init -y en la terminal de Replit.
En package.json, bajo la sección "dependencies", agrega las siguientes líneas:
"@shopify/shopify-api": "^7.0.0".
"express": "^4.17.1".
En la terminal de Replit (en la parte inferior), ejecuta el comando:
text

Contraer

Ajuste

Copiar
npm install
Abre server.js y configura el servidor con las siguientes instrucciones:
Escribe: const express = require('express'); para importar Express.
Escribe: const { Shopify } = require('@shopify/shopify-api'); para importar Shopify.
Escribe: const app = express(); para crear una instancia de Express.
Escribe: app.use(express.json()); para habilitar el parsing de cuerpos JSON.
Escribe: const shopify = new Shopify({ shopName: process.env.SHOPIFY_STORE_URL, accessToken: process.env.SHOPIFY_ACCESS_TOKEN, apiVersion: '2023-10' }); para configurar Shopify con las variables de entorno.
Escribe: app.post('/generar-enlace', async (req, res) => { para crear un endpoint POST.
Dentro del endpoint, escribe:
try { para iniciar un bloque de manejo de errores.
const { cuotas } = req.body; para extraer las cuotas del cuerpo de la solicitud.
const line_items = cuotas.map(cuota => ({ variant_id: "123456789", quantity: 1, price: (cuota.total / 100).toFixed(2) })); para crear un array de ítems basado en las cuotas (usa "123456789" como variant_id temporalmente).
const draftOrder = await shopify.rest.DraftOrder.create({ session: shopify.session, data: { line_items } }); para crear un "Draft Order".
const completedOrder = await shopify.rest.DraftOrder.complete({ session: shopify.session, id: draftOrder.id, payment_pending: true }); para completar el "Draft Order" y obtener el enlace de pago.
res.json({ paymentLink: completedOrder.invoice_url }); para enviar el enlace como respuesta JSON.
} catch (error) { para manejar errores.
res.status(500).json({ error: 'Error al generar el enlace' }); dentro del catch.
} para cerrar el bloque try/catch.
Escribe: app.listen(3000, () => console.log('Servidor en puerto 3000')); para iniciar el servidor en el puerto 3000.
Ve al panel de administración de Shopify en tu navegador: Productos > Agregar producto.
Crea un producto llamado "Pago de Cuota" con un precio base de $1.000 y guarda el producto.
En la terminal de Replit, ejecuta este comando para obtener el variant_id del producto:
text

Contraer

Ajuste

Copiar
curl -X GET "https://[mi-tienda.myshopify.com]/admin/api/2023-10/products.json" -H "X-Shopify-Access-Token: [mi Access Token]"
(Reemplaza "[mi-tienda.myshopify.com]" con el valor de SHOPIFY_STORE_URL y "[mi Access Token]" con el valor de SHOPIFY_ACCESS_TOKEN que agregaste en "Secrets").
En la respuesta JSON, busca el producto "Pago de Cuota" y copia el valor de id bajo variants (e.g., "123456789").
En server.js, reemplaza el variant_id temporal "123456789" en line_items con el valor real que copiaste (e.g., "987654321").
Abre index.html y verifica que contenga:
Un <textarea id="inputData"> para el input.
Un <button onclick="procesarDatos()">Procesar</button> para procesar el input.
Un <div id="cuotasList"> para mostrar las cuotas.
Un <span id="totalPagar"> dentro de un contenedor con clase "resumen-section" para el total a pagar.
Un <button onclick="continuarPago()">Continuar</button> en el resumen.
Abre script.js y ajusta la función continuarPago() con las siguientes instrucciones:
Escribe: async function continuarPago() { para definirla como asíncrona.
Escribe: const totalPagar = document.getElementById('totalPagar').textContent; para obtener el total.
Escribe: if (totalPagar === '$0') { alert("Por favor seleccione al menos una cuota para continuar."); return; } para validar.
Escribe: const cuotasSeleccionadas = []; para crear un array vacío.
Escribe: document.querySelectorAll('#cuotasList .cuota-item').forEach((item, index) => { para recorrer las cuotas.
Dentro del bucle, escribe:
const checkbox = item.querySelector('input[type="checkbox"]'); para obtener el checkbox.
if (checkbox.checked) { para verificar si está seleccionado.
const totalText = item.querySelector('.cuota-details p:last-child').textContent.split(': ')[1]; para extraer el "Total Cuota".
const total = parseInt(totalText.replace('$', '').replace('.', '')); para parsear el valor (e.g., "$1.359.265" → 1359265).
cuotasSeleccionadas.push({ variantId: "[mi variant_id]", quantity: 1, total }); (reemplaza "[mi variant_id]" con el valor del Paso 15).
} para cerrar el if.
Escribe: } para cerrar el bucle.
Escribe: try { para manejar errores.
Escribe: const response = await fetch('https://[nombre-de-tu-repl].repl.co/generar-enlace', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ cuotas: cuotasSeleccionadas }) }); para enviar la solicitud (reemplaza "[nombre-de-tu-repl]" con la URL pública de tu Replit).
Escribe: const data = await response.json(); para obtener la respuesta.
Escribe: window.location.href = data.paymentLink; para redirigir al enlace de pago.
Escribe: } catch (error) { para manejar errores.
Escribe: alert("Error al generar el enlace de pago"); dentro del catch.
Escribe: } para cerrar el try/catch.
En Replit, haz clic en el botón "Run" para iniciar el servidor (deberías ver "Servidor en puerto 3000" en la consola).
Copia la URL pública de Replit desde la ventana del navegador integrada (e.g., https://nombre-de-tu-repl.repl.co).
En la página web:
Pega este input en el <textarea>:
text

Contraer

Ajuste

Copiar
CRISTIAN SERVANDO VALENZUELA BUSTOS
17.546.765-3
Cuotas a pagar
Contrato
744530
PAC/PAT Activo
El pago vía PAC/PAT puede tardar hasta 5 días hábiles en verse reflejado.
** Si el cargo se hubiera realizado dentro de la fecha de pago correspondiente, no se aplicará el interés por mora señalado
Cuota N°6
Cuota
Interés Mora
Total Cuota
Venció el 05/04/2025
$1.358.270
$995
$1.359.265
Haz clic en "Procesar" para mostrar la cuota.
Marca el checkbox de la cuota.
Haz clic en "Continuar".
Verifica que el navegador redirija a un enlace de pago de Shopify (e.g., https://mi-tienda.myshopify.com/.../invoice) con el monto correcto (e.g., $13,592.65).
Si algo falla:
Abre la consola de Replit y revisa los mensajes de error.
Confirma que las credenciales en "Secrets" sean correctas.
Asegúrate de que la URL en fetch coincida con la URL pública de Replit.
Verifica que el variant_id sea válido y que el producto "Pago de Cuota" exista en Shopify.
Con estas instrucciones, el sistema debe procesar las cuotas, enviarlas al backend, generar un enlace de pago con Shopify y redirigir al usuario al hacer clic en "Continuar". Reemplaza los valores entre corchetes (e.g., "[mi-tienda.myshopify.com]", "[nombre-de-tu-repl]") con tus datos reales antes de usar este prompt.